<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.happy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Happy">
        <activity android:name=".ResetPasswordActivity"></activity>
        <activity android:name=".AnswerSecretQuestionActivity" />
        <activity android:launchMode="singleTask" android:name=".MemberProfileActivity" />
        <activity android:launchMode="singleTask" android:name=".RepProfileActivity" />
        <activity android:name=".CloseMonthActivity" />
        <activity android:name=".EnterExistingActivity" />
        <activity android:name=".AddNewRepOrEnterExistingActivity" />
        <activity android:launchMode="singleTask" android:name=".AddMeetingActivity" />
        <activity android:launchMode="singleTask" android:name=".AddShoppingItemActivity" />
        <activity android:launchMode="singleTask" android:name=".AddCleaningRoomActivity" />
        <activity android:launchMode="singleTask" android:name=".AddNewMemberActivity" />
        <activity android:launchMode="singleTask" android:name=".AddEditBillActivity" />
        <activity android:launchMode="singleTask" android:name=".ShoppingActivity" />
        <activity android:launchMode="singleTask" android:name=".MembersActivity" />
        <activity android:launchMode="singleTask" android:name=".MeetingsActivity" />
        <activity android:launchMode="singleTask" android:name=".CleaningActivity" />
        <activity android:launchMode="singleTask" android:name=".BillsActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".RegisterRepActivity" />
        <activity android:name=".RegisterUserActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".AdsActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MyRepActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>